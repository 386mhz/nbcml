import { DataStore } from '../datastore.js';
import { Utils } from '../utils.js';
import { CONFIG } from '../config.js';

export const ScheduleRenderer = {
    renderFull() {
        // ...existing schedule rendering logic...
    }
};